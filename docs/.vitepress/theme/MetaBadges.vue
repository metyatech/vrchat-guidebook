<!-- Badge display is disabled until the automation pipeline can
     auto-compute values (difficulty, time, etc.) from step metadata.
     The component and doc-before slot are kept so badges can be
     enabled without touching the theme layout. -->
<template>
  <span />
</template>
